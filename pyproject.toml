[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "autonomy-engine"
version = "1.2.0"
description = "Prefect-based autonomous build engine with human-in-the-loop decision gates"
requires-python = ">=3.10"
dependencies = [
    "prefect>=3.0",
    "anthropic",
    "openai",
    "pydantic>=2.0",
    "python-dotenv",
    "pyyaml",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "ruff",
]

[tool.setuptools.packages.find]
include = ["engine*", "flows*", "intake*", "tasks*"]

[tool.ruff]
line-length = 100
target-version = "py310"
exclude = ["customer_database"]

[tool.ruff.lint.per-file-ignores]
# load_dotenv() must run before other imports
"flows/autonomous_flow.py" = ["E402"]
"intake/intake.py" = ["E402"]

[tool.pytest.ini_options]
testpaths = ["tests"]
